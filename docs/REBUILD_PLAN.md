# План перезапуска «ДЗ»

Подготовлен 19 сентября 2026. Текущий этап: исследование завершено, следующий шаг — три варианта дизайна. В этом изменении функциональный код приложения не переделывается.

Делаем небольшой учебный планер для владельца и нескольких друзей. Сначала выбираем удобный и выразительный интерфейс, затем подключаем данные и доводим приложение до ежедневного использования. В основу новой ветки взята свежая версия Клода, а существующие ветки остаются точками возврата.

## 1. Что найдено и проверено

| Источник | Состояние на момент исследования |
|---|---|
| GitHub `main` | `ca43524`, 13 сентября, приложение 1.20.0. Это актуальная версия Клода. |
| GitHub `claude/upbeat-clarke-a448to` | Указывает на тот же `ca43524`. |
| `archive/claude-mvp-2d75887` | Первая версия от 6 сентября. Полезна как архив, но сильно отстала. |
| `codex/redesign` | `86f5a87`, 8 сентября, отдельная ранняя версия Codex. Именно она была открыта в рабочей папке до исследования. |
| `CLAUDE.md` | Краткое описание поздней версии и прежних договорённостей. Проверено по свежему коду. |
| `HANDOFF.md` | Подробный документ от 7 сентября; часть требований, поведения и списка проблем уже устарела. |
| Локальная переписка Claude | Найдены сообщения по этому проекту за 6–12 сентября. Изучены пожелания и изменения курса; полная переписка последующих чатов не установлена. |

Проверены исходники экранов, редакторов, хранения, дат, расписания, тем, жестов, PWA и публикации. Последний [запуск GitHub Actions для `ca43524`](https://github.com/ArtemChebChips/DZ/actions/runs/34756012763) завершился успешно. Локальная сборка свежей версии тоже прошла, включая TypeScript и генерацию PWA. Для неё использована отдельная копия в `.cache.local`; запись сборочных кешей потребовала запуска вне ограничений песочницы.

В ветке Codex прошли все 10 существующих тестов. Они проверяют старую модель данных и не доказывают корректность свежей версии Клода.

В локальном браузере при размере 375×812 просмотрены задачи, расписание дня, календарь, настройки и редактор. Использовались только локальные демонстрационные задания. Реальный iPhone в этом исследовании не проверялся. В прежней переписке владелец подтверждал работу офлайн, но новой версии всё равно понадобится собственная проверка на устройстве.

## 2. Что уже сделал Клод

Это работающая небольшая PWA на React, TypeScript, Vite и Tailwind, без сервера и регистрации. В ней три вкладки «Задачи / День / Настройки» и вложенные экраны календаря, редактирования расписания и предметов.

- Настоящее расписание: 11 предметов и 28 правил занятий. У каждой пары своё время, аудитория, вид и преподаватель.
- Числитель и знаменатель считаются от понедельника `2026-08-31`, а не от номера календарной недели.
- Задачи разбиты на «Просрочено / Актуальная неделя / Следующая неделя / Больше недели». Блоки сворачиваются; выполненные можно оставлять зачёркнутыми.
- Можно создать задание с предметом или без него, выполнить, вернуть, отредактировать, удалить. Есть свайпы и правка текста долгим нажатием.
- Редактор подставляет предмет с выбранной пары. По умолчанию сохраняет открытый день; кнопки «Следующая пара» и «Через одну» учитывают расписание и вид занятия.
- Свой календарь выбора даты подсвечивает подходящие занятия. Цвета предметов зависят от аттестации; лабораторные выделяются зелёным.
- Есть три темы, редакторы предметов и пар, резервная копия JSON, номер версии и механизм обновления.
- Все данные остаются на конкретном устройстве. Общей базы, файлов, фото и синхронизации пока нет.

От Codex полезны отдельные функции группировки, небольшие тесты, принцип проверки файла до импорта и уведомление об обновлении. Его старый интерфейс не является основой нового дизайна.

## 3. Что сохранить, что пересобрать

| Часть | Решение |
|---|---|
| `src/data/schedule.ts` | Сохранить актуальные данные и идентификаторы, чтобы не вводить расписание заново. |
| `src/lib/dates.ts`, `src/lib/week.ts` | Переиспользовать понятную логику, защитить небольшим набором проверок. |
| Правила задач и недель | Сохранить смысл; вынести вычисления из разметки только там, где это упрощает код и проверку. |
| Экраны, карточки, навигация, редактор, CSS | Пересобрать под выбранный дизайн. Не накладывать ещё один набор поправок поверх старой композиции. |
| Хранение и импорт | Сохранить простоту `localStorage`, исправить проверку и перенос данных. Не переносить старый модуль вслепую. |
| Жесты | Оставить обычные понятные кнопки основным способом действий; вернуть свайпы после проверки их удобства и конфликтов с прокруткой. |
| PWA и публикация | Переиспользовать рабочую основу, отдельно проверить обновления, области кешей и миграцию на новую версию. |

Сейчас для этого продукта достаточно React + TypeScript + Vite, нескольких компонентов, одного небольшого хранилища и функций для дат. Tailwind можно оставить вместе с CSS-переменными. Менять стек только ради перезапуска смысла нет. Redux, отдельный сервер, универсальные репозитории данных, микросервисы и система ролей в личной версии не нужны.

## 4. Недочёты, которые учитывать при переделке

1. **Задание теряет конкретную пару.** В браузере воспроизведено: создать задание с семинара по физике 21 сентября 2026 в 15:55 — оно показывается у лекции в 14:05. В `Task` сохраняются предмет и день, а `DayScreen` кладёт все задания на первую пару предмета. Вид занятия сохраняется только в состоянии открытого редактора. В новой версии нужна простая необязательная привязка к паре/виду занятия и понятное поведение после её удаления; точную модель определить на этапе логики.
2. **Импорт Codex несовместим с актуальными данными.** Его `parseBackup` принимает только формат 2, обязательный `subjectId` и прежнее поле цвета предмета. У Клода формат 6, `assessment` и задачи без предмета. Старый импорт нельзя использовать как готовый перенос.
3. **Импорт Клода слишком доверчив.** `importJSON` сразу передаёт разобранный JSON в `normalize` и заменяет состояние. Например, `{}` превращается в пустую базу с исходным расписанием. Ошибки записи `localStorage` скрываются. Нужны проверка до замены, понятная ошибка и сохранение исходных данных при неудаче.
4. **Миграции меняют пользовательские предметы.** Для старой версии формата `repairSubject` перезаписывает названия и аттестацию известных предметов значениями по умолчанию. Новый перенос должен сохранять правки владельца.
5. **Обновление может прервать ввод.** `main.tsx` применяет новую PWA сразу. Кнопка в настройках удаляет все service worker и кеши общего origin, включая другую версию приложения. Нужны обновление без потери открытого задания и очистка только собственных кешей.
6. **В интерфейсе тесно важным данным.** Срок и название предмета находятся в одной обрезаемой строке; длинные названия вытесняют срок. Много вложенных плашек и мелких подписей. Новый вариант должен дать сроку своё читаемое место, а не просто увеличить скругления.

Это список для новой реализации, а не поручение чинить старое приложение до выбора дизайна.

## 5. Состав первой новой версии

| Вкладка | Что в ней должно быть |
|---|---|
| **Задачи** | Список по срокам, группы недель, заметные даты и относительный срок, выполнение/возврат, создание и редактирование, сворачивание групп. Задачи без предмета поддерживаются. |
| **Расписание** | По умолчанию сегодняшний день, пары и связанные задания. Переключение дней, возврат к сегодня, доступный календарь месяца с Ч/З и сроками. Календарь остаётся внутри этой вкладки. |
| **Настройки** | Светлая/тёмная тема, показ выполненных, предметы и редактор расписания, исправление чередования недель, импорт/экспорт, версия и обновление приложения. |

Редактор задания — общая форма, не четвёртая вкладка. Редакторы предметов и расписания — вложенные экраны. Новый предмет можно создать прямо из формы пары.

Обязательные правила: отдельное время у каждой пары; даты в локальном календаре; следующая лабораторная не подменяется лекцией; изменение расписания не сдвигает сохранённый срок; в воскресенье «актуальная неделя» переключается на следующую. Отдельное поле заметки пока не возвращаем, но старые заметки при переносе не теряем.

Рабочее предположение для первого релиза — личные списки без регистрации, как в существующем приложении. Это не общий список друзей: у каждого свои данные. В старой переписке общие задания и файлы откладывались на следующий большой этап. Если владелец выберет совместную работу уже в первом релизе, после дизайна пересматриваем этап хранения и добавляем минимальный серверный сценарий.

Старые «Тихий», Telegram/iOS и «Режим сдвгшника» — история предпочтений, а не обязательная внешность. Текущая просьба — полностью новый дизайн. Сохранение третьей темы решаем после выбора основного варианта.

## 6. Этапы и результат каждого

**Этап 0 — исследование и сохранение контекста. Выполнен.**

Актуальные ветки сверены с GitHub, прочитаны код и история, выполнены базовые проверки. Создана ветка `codex/rebuild` от `ca43524`. Этот план и `AGENTS.md` фиксируют дальнейший порядок. Исходные версии остаются в Git.

**Этап 1 — три самостоятельных варианта дизайна. Следующий.**

Сделать отдельное локальное интерактивное превью на фиксированных демоданных. Оно не подключает рабочее хранилище и не регистрирует PWA. Реальное расписание можно использовать как исходное содержимое макета.

Предлагаемые направления для исследования:

| Вариант | Композиция и характер |
|---|---|
| A — «Чёткий список» | Компактные строки, отдельная колонка крупных дат, простые разделители недель, минимум вложенных контейнеров. |
| B — «Спокойный планер» | Более свободные секции дней, крупный текст задания, отчётливо отделённый срок, мягкие поверхности и меньше элементов в одном ряду. |
| C — «Графичный» | Выразительная типографика, контрастные заголовки недель и чёткие контуры, более смелая композиция при той же читаемости. |

Это направления поиска, не выбранный дизайн. У каждого показать три вкладки и редактор задания на одном наборе данных. В первом сравнении достаточно основной мобильной версии и образца тёмного экрана задач; полную проработку тем делаем для победителя.

Демонабор: просрочка, сегодня, завтра, следующая неделя, далёкий срок, выполненное, задача без предмета, длинный текст, полное название «Информационные технологии», две пары одного предмета и длинный учебный блок ВУЦ. На календаре должно быть понятно, где выбранная дата и где подходящая пара.

**Готово, когда:** варианты можно открыть и переключить, основные контролы реагируют в пределах демо, видны реальные различия композиции, приложены сопоставимые изображения с подписями перед ними. Дать свою рекомендацию с объяснением удобства. На этом этапе владелец выбирает вариант или сочетание конкретных решений.

**Этап 2 — довести выбранный дизайн.**

Собрать один цельный вариант. Уточнить типографику, плотность, цвета аттестаций, состояния кнопок, навигацию и форму задания. Подготовить светлую и тёмную темы, пустой список, длинные записи, свёрнутые и выполненные задачи. На компьютере использовать ширину осмысленно, например для календаря и дня рядом.

**Готово, когда:** все три вкладки и редактор выглядят согласованно на 375/390 px и широком экране; срок не пропадает за длинным названием; нет горизонтального скролла, перекрытий кнопкой «+» и нижней панелью; важные действия доступны без знания жестов. Проверить визуальные размеры зон нажатия — ориентир 44 px. На этом этапе утвердить внешний вид для реализации.

**Этап 3 — рабочий каркас на выбранной вёрстке.**

Перенести выбранный интерфейс в приложение, настроить три вкладки и вложенные экраны, общие кнопки и формы, переключение темы. Держать структуру небольшой: `screens`, `components`, `lib`, один модуль хранения. Общие компоненты выделять по реальному повторению.

**Готово, когда:** приложение запускается и собирается, навигация и формы работают на демоданных, утверждённый вид сохранён. Варианты дизайна остаются историей сравнения и не превращаются в три параллельных продукта.

**Этап 4 — задачи, даты и расписание.**

Подключить настоящее расписание и небольшой локальный store. Реализовать добавление, правку, выполнение/возврат и удаление задания; выбор предмета или «Без предмета»; поиск ближайшего занятия; сворачивание групп; редактирование предметов и пар.

Исправить потерю контекста пары. Сохранённые задания должны переживать изменения расписания: удалённая пара не удаляет задание, оно остаётся на своей дате в общем блоке дня. Для удаления задания дать возможность отменить действие либо явно подтвердить его. Свайпы добавлять после обычных действий и проверять отдельно от жеста смены дня.

**Готово, когда:** полный сценарий «записал → нашёл по сроку → открыл день → выполнил → вернул» работает; семинар и лабораторная не путаются с лекцией; изменения переживают перезапуск. Ключевые проверки — чередование недель, переход месяца/года, воскресенье, две пары в день, задача без предмета и неизменность дедлайна при правке расписания.

**Этап 5 — перенос данных и надёжность на телефоне.**

Сделать явный перенос резервной копии Claude формата 6; сохранить и проверить совместимость с форматом 2 старой версии Codex. Если промежуточные форматы не поддержаны, сообщать это до замены данных. Валидировать даты, ссылки на предметы и структуру; не стирать текущую базу при ошибке. Проверить сохранение пользовательских названий, выполненности, заметок и настроек. До замены предложить сохранить копию текущей базы.

Для новой версии использовать отдельные ключи данных и собственную область PWA до завершения перехода. Подключить офлайн, безопасное предложение обновления, номер версии, иконку, корректную работу с экранной клавиатурой и нижней панелью.

**Готово, когда:** импорт/экспорт проходит проверку на фикстурах без реальных личных данных; повреждённый файл не меняет базу; сбой сохранения виден пользователю; обновление не теряет черновик и не очищает чужие кеши. На реальном iPhone владелец проверяет установку, авиарежим, клавиатуру и обновление — агент не может подтвердить это одним просмотром Chromium.

**Этап 6 — публикация и переход на новую версию.**

Сейчас workflow собирает `/DZ/` из `main` и `/DZ/codex/` из `codex/redesign`. Push в новую ветку `codex/rebuild` **не публикует** её. До первого превью на GitHub Pages нужно отдельным небольшим изменением настроить сборку новой версии и её адрес, сохранив обе работающие ссылки. Точный URL превью записать здесь после настройки.

На время перехода разумно выделить `/DZ/next/` с собственной областью service worker. Это предлагаемая схема, пока она не настроена. После переноса данных и проверки на iPhone сделать новую версию основной. Упрощать совместный workflow и убирать старые публичные версии только отдельным действием в рамках перехода, сохраняя их в Git.

**Готово, когда:** сборка и публикация в Actions завершились успешно, открывается нужная версия по проверенной ссылке, есть понятный путь переноса и точка возврата. После обновления приложения — коммит и push без повторных запросов разрешения.

**Этап 7 — общие задания и материалы, если понадобятся.**

Отдельный небольшой этап после удобной личной версии: одна группа, приглашение друга, общие задания и личная отметка «выполнено». Затем файлы/фото. Перед реализацией выбрать минимальный способ входа и хранения по реальным требованиям, а не арендовать сервер заранее. Напоминания, Telegram-бот, график контрольных мероприятий и нативная оболочка остаются отдельными будущими задачами.

## 7. Как вести дальнейшую работу

Один запрос — один законченный этап или небольшой сценарий. Следующий запрос: «Сделай этап 1 из `docs/REBUILD_PLAN.md`». Примеры более точных промптов находятся в `docs/ASTRA_WORKFLOW.md`.

После этапа записывать здесь его статус и принятые решения. Проверять только затронутые сценарии и обязательную сборку; не запускать бесконечные повторные проверки после успешного результата. У изменений документации своя проверка, номер приложения от них не меняется.

Неизменные критерии: приложение быстро отвечает на «что сдавать и когда», «какие пары в этот день», «как записать новое задание»; код остаётся понятным; новая функция появляется под реальную потребность владельца.
